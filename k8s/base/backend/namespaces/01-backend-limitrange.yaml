apiVersion: v1
kind: LimitRange
metadata:
  name: backend-limit-range
  namespace: backend-ns
spec:
  limits:
  - default:
      cpu: "500m"
      memory: "256Mi"
    defaultRequest:
      cpu: "100m"
      memory: "64Mi"
    type: Container
