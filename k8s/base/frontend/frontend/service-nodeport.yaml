apiVersion: v1
kind: Service
metadata:
  name: frontend-nodeport
  namespace: frontend-ns
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080
    protocol: TCP
    name: http
  selector:
    app: frontend
