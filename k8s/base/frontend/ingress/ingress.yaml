apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cinema-ingress
  namespace: frontend-ns
spec:
  ingressClassName: nginx
  rules:
  - host: cinema.mazadonia.cc
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend
            port:
              number: 80
