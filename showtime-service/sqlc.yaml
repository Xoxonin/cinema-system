version: "2"
sql:
  - engine: "postgresql"
    queries: "db/query/query.sql"
    schema: "db/migration"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_json_tags: true
